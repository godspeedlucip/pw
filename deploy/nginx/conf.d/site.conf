server {
    listen 80;
    server_name godspeedlucip.top www.godspeedlucip.top;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}


## 目前因为没有申请ssl证书，所以先不监听Https 443端口，等申请到证书后再放开下面的代码
# server {
#     listen 443 ssl http2;
#     server_name godspeedlucip.top www.godspeedlucip.top;

#     ssl_certificate /etc/letsencrypt/live/godspeedlucip.top/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/godspeedlucip.top/privkey.pem;

#     client_max_body_size 25m;

#     location / {
#         proxy_pass http://next-app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
